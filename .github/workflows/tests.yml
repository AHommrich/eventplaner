name: tests

on:
  push:
    branches:
      - develop
      - staging
      - production
  pull_request:
    branches:
      - develop
      - staging
      - production

jobs:
  ci:
    runs-on: ubuntu-latest

    services:
      mariadb:
        image: mariadb:11
        env:
          MARIADB_ROOT_PASSWORD: root
          MARIADB_DATABASE: laravel_test
          MARIADB_USER: laravel
          MARIADB_PASSWORD: secret
        ports:
          - 3306:3306
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Laravel's MariaDbSchemaState invokes the `mariadb` CLI to load the
      # schema dump. ubuntu-latest ships only the mysql-client by default, so
      # install the mariadb-client explicitly.
      - name: Install MariaDB client
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends mariadb-client

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.3
          tools: composer:v2
          coverage: xdebug

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Get Composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Install PHP Dependencies
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Install Node Dependencies
        run: npm ci

      # Surface CVEs in the dependency tree. Non-blocking during the
      # initial rollout so third-party advisories cannot turn CI red
      # before the maintainer has triaged them. Flip continue-on-error
      # to false after ~4-6 weeks of clean runs.
      - name: Composer security audit
        run: composer audit --format=plain
        continue-on-error: true

      - name: npm security audit
        run: npm audit --omit=dev --audit-level=high
        continue-on-error: true

      - name: Copy Environment File
        run: cp .env.example .env

      - name: Generate Application Key
        run: php artisan key:generate

      - name: Publish Ziggy Configuration
        run: php artisan ziggy:generate

      - name: Typecheck (vue-tsc)
        run: npm run typecheck

      - name: Build Assets
        run: npm run build

      - name: Frontend Tests (Vitest)
        run: npm run test:coverage

      - name: Backend Tests (Pest + coverage)
        env:
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_USERNAME: laravel
          DB_PASSWORD: secret
        run: ./vendor/bin/pest --ci --coverage --coverage-clover=coverage/clover.xml
